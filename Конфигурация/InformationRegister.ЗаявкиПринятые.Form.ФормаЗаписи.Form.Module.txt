&НаКлиенте
Процедура ВвестиСобытие(Команда)
	ОткрытьФорму("РегистрСведений.События.ФормаЗаписи",ВвестиСобытиеНаСервере());
КонецПроцедуры

&НаСервере
функция ВвестиСобытиеНаСервере()
	// Вставить содержимое обработчика.
	ПарЗаполнения = новый структура;
	//ПарЗаполнения.Вставить("ДатаЗаявки",ЭтотОбъект.Запись.Дата);
	//ПарЗаполнения.Вставить("НомерЗаявки",ЭтотОбъект.Запись.Номер);
	//ПарЗаполнения.Вставить("Контрагент",Лев (ЭтотОбъект.Запись.Контрагент,20));
	возврат ПарЗаполнения;
	
КонецФункции
